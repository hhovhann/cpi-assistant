# Tests run without Docker, LM Studio or the internet:
# - the in-memory vector store (PgVectorStoreTest starts its own pgvector container)
# - no SAP Help pages saved at startup
cpi.store.type=memory
cpi.knowledge.seed-on-startup=false
