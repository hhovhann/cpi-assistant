# Answer evaluation set.
#
# Format:  question | fact; fact; fact
#   - Each fact must appear in a correct answer (case-insensitive).
#   - A fact can list alternatives with "/":  Keystore/key store
#   - Empty right-hand side = off-topic: the right answer is "I don't know".
#
# Facts are deliberately few and specific — the words a correct answer cannot
# avoid. They must not be words the question already contains ("email"), or
# short strings that hide inside other words ("ID" in "inside").
# Keyword matching is crude: a right answer phrased differently can fail it,
# and a wrong answer that mentions the word can pass it. Read the answers in
# the report, don't just trust the percentages.

How do I connect to a database from an iFlow? | JDBC; data source; receiver
What method does my Groovy code have to implement? | processData
When should I put code inline instead of in a separate script file? | 30; short/simple
How do I send an alert email when a message fails? | Exception Subprocess
How do I get a receipt proving my partner received the message? | MDN; AS2
How do I break a batch of EDI documents into single messages? | EDI Splitter
How do I avoid processing the same message twice? | Data Store; already/exists/duplicate
Where are certificates and private keys managed? | Keystore
How do I send a message to different receivers depending on its content? | Router; condition
How does a buyer shop in a supplier's catalog from their procurement system? | PunchOut; cXML

# Off-topic. The JVM question scores above the retrieval floor (see 10a),
# so it tests whether the prompt rule holds when retrieval lets noise through.
What is the capital of France? |
How do I tune garbage collection in the JVM? |
